#!/usr/bin/env bash
# is-elevated — exit 0 if running as root, 1 otherwise. Prints true/false.
# Ported from Test-IsElevated (non-Windows branch).
set -euo pipefail
if [[ "$(id -u)" -eq 0 ]]; then
    echo true; exit 0
else
    echo false; exit 1
fi
